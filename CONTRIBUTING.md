# 📜 Contribution Guidelines (Temporal Semiotics)## 🔹 Как добавить новый ПодАкт (N.1, N.2...)1. Скопируйте шаблон: `cp contracts/templates/act_N.0_template.yaml contracts/domains/TS-<Domain>_N.0/subacts/<topic>_N.1.yaml`2. Заполните `based_on`, `levels`, `language_contract`, `metrics`.3. Запустите валидацию: `./scripts/run_full_validation_pipeline.sh`4. При `7 passed` → закоммитьте и создайте PR.## 🔹 Конституционные правила- ❌ Не меняйте `Z-правила` ядра (Z-0, Z_Ω, Z_R.1–Z_R.6).- ✅ Новые доменные Z-правила должны иметь префикс `Z_<Domain>.X`.- ✅ ΔΓ вычисляется только при `status: "validated"`.- ✅ CI не калибрует ε — только верифицирует `ε ∈ C-rules`.## 🔹 Тестирование```bashsource venv/bin/activatepython -m pytest tests/test_validator.py -vpython3 runtime/ts_physics_mvp.py🔹 СвязьПроект развивается рекурсивно. Предложения по эволюции ядра проходят через X_Upgrade_Protocol.📩 maxdobrohot@gmail.com